# 2주 트라이얼 로그 (2026-08-12 ~ 2026-08-26)

> 이 파일은 `notes/` 아래에서 유일하게 한국어다. 나머지 리뷰·계획 문서는 영어가
> house style이지만, 이것은 Ricky가 매일 손으로 채우는 체크리스트이고 필드 정의가
> 한국어인 [MANUAL-TASKS §8](../MANUAL-TASKS.md)에 있다. "영어로 통일"하지 말 것.

**창: 2026-08-12 00:00 UTC ~ 2026-08-26.** [PREREGISTRATION §8.5](../PREREGISTRATION.md)에
고정돼 있다. 이 로그가 그 게이트 판정의 입력이다.

창 안의 KRX 세션은 10일이다 — 08-12, 08-13, 08-14, 08-18, 08-19, 08-20, 08-21,
08-24, 08-25, 08-26. 08-17은 광복절 대체휴일이라 빠진다. 브리핑은 세션당 아침·저녁
두 번이므로 판정 대상 리포트는 최대 20건이다.

**이 2주 동안은 브리핑을 근거로 매매를 바꾸지 말 것.** 표본이 정보를 담기에는 턱없이
작고, 그에 따라 행동하면 측정이 편향으로 바뀐다.

---

## 매일 적는 것

MANUAL-TASKS §8의 다섯 항목 그대로다. 아래 템플릿을 복사해서 날짜만 바꿔 쓴다.
**빈칸으로 두는 것보다 "안 읽음"이라고 적는 것이 훨씬 낫다** — 8일차쯤 안 읽게 됐다면
그게 이 트라이얼의 가장 중요한 발견이다.

```
### YYYY-MM-DD

- 읽었나:
- ⑧만 읽었나:
- ⑧이 모순으로 삭제됐나:
- 이상해 보인 것:
- ambiguous 비율 / 실행 비용:
```

---

## 일자별 기록

> **2026-08-12~08-21(일곱 세션, 14건)은 2026-08-22에 소급으로 채워졌다.** 이 파일이
> 스스로 경고했던 실패가 실제로 벌어졌다 — 창이 열린 뒤 열흘 동안 08-12 칸 하나도
> 채워지지 않은 채 방치됐다(2026-08-22 프로젝트 리뷰에서 발견). `읽었나` / `⑧만
> 읽었나` / `이상해 보인 것`은 당시 기록을 남기지 않아 지금 정확히 재현할 수 없으므로,
> 지어내는 대신 정직하게 **기록 없음**으로 남긴다 — 빈칸보다 "기록 없음"이 낫다는
> 이 파일의 원칙을 그대로 적용한 것이다. `⑧이 모순으로 삭제됐나`와 `ambiguous 비율`은
> 커밋된 `reports/`에서 객관적으로 재구성 가능해 채웠다. 08-22는 KRX 세션이 아니므로
> (주말) 다음 세션인 08-24부터는 그날그날 실시간으로 채운다.

### 2026-08-12

- 읽었나: 기록 없음 (소급 작성, 당시 기록 없음)
- ⑧만 읽었나: 기록 없음 (위와 동일)
- ⑧이 모순으로 삭제됐나: 해당 없음 — ⑧ 자체가 아직 생성되지 않음(LLM 단계 미완성, placeholder — 리포트 헤더 `⚠ 미구현 섹션`에 명시)
- 이상해 보인 것: 기록 없음
- ambiguous 비율 / 실행 비용: 아침 7.2% (기사 1,062건) / 저녁 7.7% (기사 1,153건) · 실행 비용 $0 (LLM 스코어링 단계 미가동)

### 2026-08-13

- 읽었나: 기록 없음 (소급 작성, 당시 기록 없음)
- ⑧만 읽었나: 기록 없음 (위와 동일)
- ⑧이 모순으로 삭제됐나: 해당 없음 — ⑧ 자체가 아직 생성되지 않음(LLM 단계 미완성, placeholder)
- 이상해 보인 것: 기록 없음
- ambiguous 비율 / 실행 비용: 아침 8.1% (기사 1,240건) / 저녁 9.2% (기사 1,190건) · 실행 비용 $0 (LLM 스코어링 단계 미가동)

### 2026-08-14

- 읽었나: 기록 없음 (소급 작성, 당시 기록 없음)
- ⑧만 읽었나: 기록 없음 (위와 동일)
- ⑧이 모순으로 삭제됐나: 해당 없음 — ⑧ 자체가 아직 생성되지 않음(LLM 단계 미완성, placeholder)
- 이상해 보인 것: 기록 없음
- ambiguous 비율 / 실행 비용: 아침 9.4% (기사 1,257건) / 저녁 12.9% (기사 1,031건) · 실행 비용 $0 (LLM 스코어링 단계 미가동)

### 2026-08-18

- 읽었나: 기록 없음 (소급 작성, 당시 기록 없음)
- ⑧만 읽었나: 기록 없음 (위와 동일)
- ⑧이 모순으로 삭제됐나: 해당 없음 — ⑧ 자체가 아직 생성되지 않음(LLM 단계 미완성, placeholder)
- 이상해 보인 것: 기록 없음
- ambiguous 비율 / 실행 비용: 아침 12.8% (기사 1,105건) / 저녁 9.0% (기사 1,084건) · 실행 비용 $0 (LLM 스코어링 단계 미가동)

### 2026-08-19

- 읽었나: 기록 없음 (소급 작성, 당시 기록 없음)
- ⑧만 읽었나: 기록 없음 (위와 동일)
- ⑧이 모순으로 삭제됐나: 해당 없음 — ⑧ 자체가 아직 생성되지 않음(LLM 단계 미완성, placeholder)
- 이상해 보인 것: 기록 없음. 참고: 이날 저녁 리포트에 `macro/coverage` 데이터 정합성 항목이 있었다 — [PREREGISTRATION §8.5 기준 2](../PREREGISTRATION.md)의 `ambiguous` 분류 대상(§R 2026-08-22 항목 참고)
- ambiguous 비율 / 실행 비용: 아침 9.0% (기사 1,161건) / 저녁 9.8% (기사 974건) · 실행 비용 $0 (LLM 스코어링 단계 미가동)

### 2026-08-20

- 읽었나: 기록 없음 (소급 작성, 당시 기록 없음)
- ⑧만 읽었나: 기록 없음 (위와 동일)
- ⑧이 모순으로 삭제됐나: 해당 없음 — ⑧ 자체가 아직 생성되지 않음(LLM 단계 미완성, placeholder)
- 이상해 보인 것: 기록 없음
- ambiguous 비율 / 실행 비용: 아침 9.5% (기사 1,057건) / 저녁 9.1% (기사 1,030건) · 실행 비용 $0 (LLM 스코어링 단계 미가동)

### 2026-08-21

- 읽었나: 기록 없음 (소급 작성, 당시 기록 없음)
- ⑧만 읽었나: 기록 없음 (위와 동일)
- ⑧이 모순으로 삭제됐나: 해당 없음 — ⑧ 자체가 아직 생성되지 않음(LLM 단계 미완성, placeholder)
- 이상해 보인 것: 기록 없음
- ambiguous 비율 / 실행 비용: 아침 9.0% (기사 1,092건) / 저녁 5.9% (기사 848건) · 실행 비용 $0 (LLM 스코어링 단계 미가동)

> **08-24부터 "읽었나"의 의미가 바뀐다.** ⑤·⑧이 아직 없는 동안 이 두 항목을 안 읽는
> 것은 습관 이탈이 아니라 **읽을 대상이 아직 없다는 뜻**이다 — Ricky가 08-25에
> 확인한 바로는 의도적 보류다. 이 파일이 경고했던 "8일차쯤 안 읽게 됐다"는 시나리오는
> 완성된 포맷을 습관적으로 건너뛰는 상황을 가리킨 것이지, 미구현 섹션을 기다리는
> 상황과는 다르다 — 둘을 같은 "기록 없음"으로 뭉치지 않는다.

### 2026-08-24

- 읽었나: 아니오 — 의도적 보류(⑤·⑧ 미구현이라 읽을 실익이 적다는 판단, Ricky 확인)
- ⑧만 읽었나: 해당 없음 — ⑧ 자체가 아직 생성되지 않음
- ⑧이 모순으로 삭제됐나: 해당 없음 — 위와 동일
- 이상해 보인 것: 없음
- ambiguous 비율 / 실행 비용: 아침 6.0% (기사 913건) / 저녁 9.0% (기사 941건) · 실행 비용 $0 (LLM 스코어링 단계 미가동)

### 2026-08-25

- 읽었나: 아니오 — 의도적 보류(위와 동일)
- ⑧만 읽었나: 해당 없음 — ⑧ 자체가 아직 생성되지 않음
- ⑧이 모순으로 삭제됐나: 해당 없음 — 위와 동일
- 이상해 보인 것: 없음
- ambiguous 비율 / 실행 비용: 아침 8.4% (기사 1,023건) / 저녁 9.0% (기사 947건) · 실행 비용 $0 (LLM 스코어링 단계 미가동)

---

## 누적 — 게이트 기준

2026-08-26에 판정하는 것은 §8.5 네 기준 중 **셋**이다. 네 번째(모델간 polarity 상관
> 0.5)는 8단계 베이크오프가 있어야 재므로 그때 따로 잰다. **둘 다 통과해야 통과다.**

| 기준 | 상태 | 근거 |
|---|---|---|
| 파이프라인 무중단 | | |
| 데이터 정합성 오류 0건 | | |
| `ambiguous` < 30% | | |

**무중단**은 세 조건이다 — ① 뜬 실행은 전부 실행 파일을 남겼나 ② 설명되지 않은
`check_feed_continuity` 실패가 없나 ③ 같은 원인으로 연속 2회 검증 실패가 없나.

**정합성 오류**로 세는 것은 네 가지뿐이다 — `schema`, `structural_invariants`,
`flow_identity`, 그리고 미국 시세 벤더 불일치. 리포트 헤더의 `⚠ 수집 실패:` 줄에서
읽는다. `missing_ratio`와 `trading_day_continuity`는 세지 않는다(각각 완전성 문제와
가용성 사고의 부산물이다). 자세한 근거는 §8.5.

**`ambiguous` 비율**은 위 일자별 기록의 값을 창 전체로 평균한다.

---

## 참고값 — 판정에 안 들어감

여기 적는 것은 게이트 기준이 **아니다.** 판정과 함께 보고만 한다.

| 항목 | 값 |
|---|---|
| 선언 대비 실제 실행률 | |
| 실행 비용 합계 | |

선언 대비 실행률을 기준으로 삼지 않는 이유는 실행을 흘리는 쪽이 GitHub 스케줄러이지
이 파이프라인이 아니기 때문이다. 2026-08-03~07 실측은 42.6%였다.

---

## 창이 끝나면

1. 위 표 세 칸을 채우고, 통과·미달을 그대로 적는다. 미달이면 §8.5의 "신호 작업 중단,
   측정 레이어 수리"가 발동한다.
2. 8단계가 올라온 뒤 모델간 상관을 같은 기간 기사로 재고, 그 결과를 여기 덧붙인다.
3. 포맷에 대한 발견(⑧만 읽었다, 특정 섹션이 잡음이었다)은 신호 문제가 아니라 포맷
   문제다. 게이트와 섞지 말고 따로 정리한다.
